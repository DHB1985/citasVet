import React from "react";
import { Text, View, SafeAreaView } from "react-native";

const DataPatientModal = () => {
  return (
    <SafeAreaView>
      <Text>DataPatientModal</Text>
    </SafeAreaView>
  );
};

export default DataPatientModal;
